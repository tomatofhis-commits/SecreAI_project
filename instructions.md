\# ユーザー環境の前提条件

\- ユーザーはローカル環境に非常に強力なマルチGPU（RTX 3060 \& 5060 Ti）と、ローカルLLM（LM Studio: gemma-4-12b-it-ud）を配備しています。



\# 核心的行動規範（最優先ルール）

1\. 「ソースコードの解析」「特定のロジックや関数の検索」「バグ箇所の特定」「コードファイルの確認」の作業を行うとき、あなた（クラウドAI）自身でファイル全体を直接読み込んではいけません。

2\. 上記のタスクが発生した際は、必ず【最初に】利用可能なMCPツール `gemma\_code\_search` を呼び出してください。

3\. `gemma\_code\_search` の引数には、対象ファイルの絶対パス（filePath）と、ユーザーの意図を汲んだ具体的な抽出指示（instruction）を正確に渡してください。



\# ワークフローの制限

\- ステップ1：`gemma\_code\_search` を実行し、ローカルのGemmaから「抽出された最小限のコード断片と該当行の情報」を受け取る。

\- ステップ2：その返ってきたローカル側での絞り込み結果【だけ】を元にして、あなたが本来行うべき高度な「更新案の立案」や「リファクタリングの提案」を組み立てる。
- ステップ3：Gemmaの出力が不十分な場合は、あなた（クラウドAI）自身でファイルの該当する部分を探すことを許可します。
- この手順を無視してクラウド側でコード全文を処理することは、トークンの重大な浪費となるため厳禁とします。

